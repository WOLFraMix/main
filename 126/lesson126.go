package main

func main() {
	/*
	   slices.Sort(s) — сортирует слайс s в порядке возрастания.
	   Работает для любых типов, которые поддерживают сравнение (например, числа, строки).
	   Пример: slices.Sort(int{5, 2, 8}) → [2, 5, 8].

	   slices.Contains(s, v) — проверяет, есть ли элемент v в слайсе s.
	   Возвращает true или false.
	   Пример: slices.Contains(int{1, 2, 3}, 2) → true.

	   slices.Delete(s, i, j) — удаляет из слайса s элементы начиная с i и до j, не включая j.
	   Возвращает новый слайс.
	   Пример: slices.Delete(int{1, 2, 3, 4}, 1, 3) → [1, 4].

	   slices.Insert(s, i, elements...) — вставляет элементы elements в слайс s начиная с индекса i.
	   Возвращает новый слайс.
	   Пример: slices.Insert(string{"a", "c"}, 1, "b") → ["a", "b", "c"].

	   slices.Clone(s) — создаёт поверхностную копию слайса s.
	   Полезно, когда нужно работать с данными, не изменяя оригинал.

	   slices.Compact(s) — удаляет из слайса последовательные повторяющиеся элементы, оставляя только по одному экземпляру.
	   Пример: slices.Compact(string{"a", "a", "b", "b", "b", "c"}) → ["a", "b", "c"].

	   slices.BinarySearch(s, target) — ищет target в отсортированном слайсе s.
	   Возвращает индекс элемента и булево значение: true, если элемент найден, false — если нет.

	   slices.All(s) и slices.Backward(s) — возвращают итераторы для прохода по слайсу.
	   Первый идёт от начала к концу, второй — от конца к началу.
	   Это полезно, когда нужно совместить итерацию с другими операциями (например, с фильтрацией).
	*/
}
